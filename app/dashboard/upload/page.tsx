import UploadForm from "./UploadForm";

type Props = {};

const Page = ({}: Props) => {
	return (
		<main>
			<h1 className="mb-12">Upload Course</h1>
			<UploadForm />
		</main>
	);
};

export default Page;
